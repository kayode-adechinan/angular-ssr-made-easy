import { ScullyConfig } from '@scullyio/scully';
export const config: ScullyConfig = {
  projectRoot: "./src",
  projectName: "angular-firebase-authentication",
  outDir: './dist/static',
  routes: {
  }
};